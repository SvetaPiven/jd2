package com.avia.repository.columns;

    public enum PassDoc {
        ID_PASS,
        FULL_NAME,
        PERSONAL_ID,
        ID_DOCUMENT_PASS,
        ID_DOCUMENT_TYPE,
        DOCUMENT_NUM,
        CREATED,
        CHANGED,
        IS_DELETED;
    }
